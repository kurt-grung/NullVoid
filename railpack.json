{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["npm ci && npm run api:build"]
    },
    "build": {
      "commands": ["true"]
    }
  },
  "deploy": {
    "startCommand": "node packages/api/dist/index.js"
  }
}
