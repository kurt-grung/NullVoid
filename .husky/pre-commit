npx lint-staged

# Optional: run NullVoid security scan before commit (set NULLVOID_PRE_COMMIT=1 to enable)
if [ "$NULLVOID_PRE_COMMIT" = "1" ]; then
  npm run build && npx nullvoid . --format text --depth 2
fi
