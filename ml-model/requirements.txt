# CI uses Python 3.11 (.github/workflows/tests.yml). scikit-learn 1.8+ needs Python 3.11+.
scikit-learn>=1.3.0,<2.0.0
joblib>=1.3.0
xgboost>=2.0.0,<3.0.0
fastapi>=0.104.0
uvicorn>=0.24.0
# Optional: pip install shap  (for per-sample explanations via POST /explain)
