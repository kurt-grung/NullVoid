{
  "version": 2,
  "buildCommand": "npm ci && VITE_BASE=/ npm run dashboard:build",
  "outputDirectory": "packages/dashboard/dist",
  "rewrites": [
    {
      "source": "/((?!assets/|vite\\.svg).*)",
      "destination": "/index.html"
    }
  ]
}
